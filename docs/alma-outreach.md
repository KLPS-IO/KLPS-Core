# Alma Angels distribution and update tracking

Alma is a separate channel in the existing fundraising_engagements table, not the FounderCatalyst engagement and not an accounting funding entry. One correspondence event links the original verified email/version and fixes the evidenced stage at DISTRIBUTED TO INVESTOR COMMUNITY on 17 September 2026. The original Gmail PDF has private header provenance; the displayed send and export timestamps are separate, and the email timezone is unstated. General Alma ticket/community figures remain attributed source context, never KLPS metrics.

GET /api/finance/outreach is behind the existing founder_admin finance guard. It deliberately projects limited channel/activity fields, omitting email headers and source hashes. Stale source evidence removes the verified stage. POST /api/finance/outreach/:id/updates only records evidenced milestone candidates with a fixed DRAFT — FOUNDER APPROVAL REQUIRED state. It rejects approval/sending, stage, investor and financial fields. There is no send, pipeline advancement, investor creation, accounting or capital action in this service.

Future actual interactions should reference this channel and distribution activity, then separately evidenced contact, interaction/meeting, diligence, proposal, agreement, cash reconciliation and approved corporate-event records. These are references between independently controlled records, never automatic side effects. Do not create missing investor/opportunity entities now. Retain distinct commitment, signed agreement, cash receipt and share issuance events; neither distribution nor an update candidate establishes them.

The operator-only ingestion script uses existing company and founder identities and content-addressed original evidence, adds its initial evidence_versions snapshot, and appends a correspondence event. Advisory locking and source key/checksum checks prevent duplicate ingestion. The manifest containing headers and file paths stays outside Git. When the production hold is active, ingestion targets readiness_hold_20260917 only. Its records return with the already-reviewed restore operation after the new backend privacy controls are live. No new tables, scheduled jobs, outbound integrations or migrations are introduced.

Validation: unit input guards, database ingestion/idempotency/stale-version checks, unchanged cap table/funding/users, real HTTP investor denial and signed-source protections, and desktop/mobile UI tests. UI test fixtures are removed before commit. Source files, recipient/sender details and hashes must never be placed in frontend configuration or investor documents.
